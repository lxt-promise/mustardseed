# 视频翻译配音工作台 - 依赖
# 均为纯 Python 包，Windows 下有预编译 wheel，无需编译器

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
aiofiles>=23.2

# 语音合成（微软 Edge TTS，免费）
edge-tts>=6.1.9

# 本地语音识别（可选：不装则只能用云端识别）
faster-whisper>=1.0.0

# 硬字幕 OCR（可选：从画面提取烧录字幕）
rapidocr_onnxruntime>=1.3.0
